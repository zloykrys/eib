# Define the absolute path to the VM files storage $VMFOLDER
# assuming the eib-image iso is in the root of the storage folder
# and VM files are under $VMFOLDER/qcows
export VMFOLDER=/home/user/eib

virt-install \
  --connect qemu:///system \
  --name rke2-1 \
  --memory 8192 \
  --vcpus 8 \
  --cpu host-passthrough \
  --disk path=$VMFOLDER/qcows/rke2-1.qcow2,size=60,format=qcow2,bus=virtio \
  --os-variant slem6.2 \
  --network network=host-nat,model=virtio,mac=34:8A:B1:4B:16:E1 \
  --cdrom $VMFOLDER/eib-image.iso \
  --graphics vnc \
  --boot uefi,hd,cdrom \
  --noautoconsole
  
  
virt-install \
  --connect qemu:///system \
  --name rke2-2 \
  --memory 8192 \
  --vcpus 8 \
  --cpu host-passthrough \
  --disk path=$VMFOLDER/qcows/rke2-2.qcow2,size=60,format=qcow2,bus=virtio \
  --os-variant slem6.2 \
  --network network=host-nat,model=virtio,mac=34:8A:B1:4B:16:E2 \
  --cdrom $VMFOLDER/eib-image.iso \
  --graphics vnc \
  --boot uefi,hd,cdrom \
  --noautoconsole
  
virt-install \
  --connect qemu:///system \
  --name rke2-3 \
  --memory 8192 \
  --vcpus 8 \
  --cpu host-passthrough \
  --disk path=$VMFOLDER/qcows/rke2-3.qcow2,size=60,format=qcow2,bus=virtio \
  --os-variant slem6.2 \
  --network network=host-nat,model=virtio,mac=34:8A:B1:4B:16:E3 \
  --cdrom $VMFOLDER/eib-image.iso \
  --graphics vnc \
  --boot uefi,hd,cdrom \
  --noautoconsole
  

To test the deployment, open console in virt-manager, login as root/eib
  
export PATH=$PATH:/var/lib/rancher/rke2/bin/
export KUBECONFIG=/etc/rancher/rke2/rke2.yaml
kubectl get nodes


